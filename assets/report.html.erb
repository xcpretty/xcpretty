<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Test Results | xcpretty</title>
    <style type="text/css">
      h1 {}
      .left {}
      .right {}
      .test-suite {}
      .test-suite .heading {}
      .test-suite .heading .title {}
      .test {}
      .test.failing {}
      .test.passing {}
      .test .heading {}
      .test .heading .title {}
      .test .heading .time {}
      .snippet-control {}
      .snippet-control.collapsed {}
      .snippet {}
      .snippet.hidden {}
      #segment-bar {}
      #segment-bar > a {}
      #segment-bar > a.selected {}
      #counters {}
      #test-count {}
      #fail-count {}
    </style>
  </head>
  <body>
    <header>
      <section class="left">
        <h1>Test Results</h1>
      </section>
      <section class="right">
        <section id="counters">
          <h2 id="test-count"></h2>
          <h2 id="fail-count"></h2>
        </section>
        <section id="segment-bar">
          <a class="selected">All</a><a>Failing</a><a>Passing</a>
        </section>
      </section>
    </header>
    <section id="test-suites">
      <% test_suites.each do |name, info| %>
        <% next unless info[:tests].size > 0 %>
        <section class="test-suite <%= info[:failing] ? 'failing' : 'passing'%>" id="<%= name %>">
          <section class="heading">
            <h3 class="title"><%= name %></h3>
          </section>
          <section class="tests">
            <% info[:tests].each do |test| %>
              <section class="test <%= test[:failing] ? 'failing' : 'passing'%>" id="<%= test[:name] %>">
                <section class="heading">
                  <% if test[:snippet] %>
                    <input type="button" class="snippet-control collapsed"/>
                  <% end %>
                  <h3 class="title"><%= test[:name] %></h3>
                  <% if test[:time] %>
                    <h3 class="time"><%= test[:time] %></h3>
                  <% end %>
                </section>
                <% if test[:snippet] %>
                  <section class="snippet hidden"><%= test[:snippet] %></section>
                <% end %>
              </section>
            <% end %>
          </section>
        </section>
      <% end %>
    </section>
    <footer>Report generated with <a href="https://github.com/mneorr/xcpretty">xcpretty</a></footer>
  </body>
</html>